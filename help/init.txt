init    加载当前目录下的仓库
      [newrepo|-exit|-m|-rm]

      newrepo    (可选)在当前目录建立新仓库
      -exit      (可选)退出init状态
      -m         (可选)加载当前目录下的仓库的同时将这个仓库加入monitor的监管
      -rm        (可选)加载当前目录下的仓库的同时将这个仓库从monitor的监管中移除